# Screenshots

Drop a screenshot of the interactive menu running here, named `menu-demo.png`,
and it will appear in the main README.

Suggested capture: run `python src/vaultone_bulk.py`, let the menu render, and
screenshot the terminal showing the resource-type and operation choices plus a
pre-flight check.
